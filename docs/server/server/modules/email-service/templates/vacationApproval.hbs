<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: {{#if approved}}#4CAF50{{else}}#f44336{{/if}};
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9f9f9;
      padding: 20px;
      border: 1px solid #ddd;
      border-top: none;
    }
    .status-badge {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 20px;
      font-weight: bold;
      margin: 10px 0;
      background-color: {{#if approved}}#4CAF50{{else}}#f44336{{/if}};
      color: white;
    }
    .details {
      background-color: white;
      padding: 15px;
      margin: 15px 0;
      border-left: 4px solid {{#if approved}}#4CAF50{{else}}#f44336{{/if}};
    }
    .details-row {
      margin: 10px 0;
    }
    .label {
      font-weight: bold;
      color: #555;
    }
    .footer {
      text-align: center;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      color: #777;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Vacation Request {{#if approved}}Approved{{else}}Rejected{{/if}}</h1>
  </div>
  
  <div class="content">
    <p>Hello {{employeeName}},</p>
    
    <p>Your vacation request has been <span class="status-badge">{{#if approved}}APPROVED{{else}}REJECTED{{/if}}</span></p>
    
    <div class="details">
      <div class="details-row">
        <span class="label">Start Date:</span> {{startDate}}
      </div>
      <div class="details-row">
        <span class="label">End Date:</span> {{endDate}}
      </div>
      <div class="details-row">
        <span class="label">Duration:</span> {{duration}} days
      </div>
      <div class="details-row">
        <span class="label">Type:</span> {{vacationType}}
      </div>
      {{#if approved}}
      <div class="details-row">
        <span class="label">Remaining Balance:</span> {{remainingBalance}} days
      </div>
      {{/if}}
      {{#if comments}}
      <div class="details-row">
        <span class="label">Comments:</span> {{comments}}
      </div>
      {{/if}}
    </div>
    
    {{#if approved}}
    <p>Your vacation has been approved. Please ensure all your work is properly handed over before your leave begins.</p>
    {{else}}
    <p>Unfortunately, your vacation request could not be approved at this time. Please contact your manager for more information.</p>
    {{/if}}
    
    {{#if actionUrl}}
    <p style="text-align: center; margin: 20px 0;">
      <a href="{{actionUrl}}" style="background-color: {{#if approved}}#4CAF50{{else}}#2196F3{{/if}}; color: white; padding: 12px 30px; text-decoration: none; border-radius: 5px; display: inline-block;">View Details</a>
    </p>
    {{/if}}
  </div>
  
  <div class="footer">
    <p>This is an automated message from the HR Management System.</p>
    <p>Please do not reply to this email.</p>
  </div>
</body>
</html>
