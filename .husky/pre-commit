#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Check HR-Core boundaries
echo "🔍 Checking HR-Core module boundaries..."

# Run ESLint on HR-Core files
npx eslint server/modules/hr-core/**/*.js --quiet

if [ $? -ne 0 ]; then
  echo "❌ HR-Core boundary violation detected!"
  echo "🚨 HR-Core CANNOT depend on optional modules!"
  echo "Please remove imports from optional modules in HR-Core."
  exit 1
fi

echo "✅ HR-Core boundaries are intact"
